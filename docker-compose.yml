services:
  catalog-service:
    build:
      context: ./services/catalog-service
    ports:
      - "8000:8000"
    healthcheck:
      test: ["CMD", "/healthcheck.sh"]
      interval: 130s
      timeout: 130s
      retries: 1
